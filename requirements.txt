# Web framework
flask>=3.1.3
flask-wtf
flask-cors
werkzeug>=3.1.0

# Database
pymongo
bcrypt

# Authentication & security
PyJWT
python-dotenv

# AI / LLM
groq

# RAG / Vector search
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-core>=0.2.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

# Data processing
pandas
numpy

# NLP / Fuzzy matching
thefuzz[speedup]
python-Levenshtein

# HTTP client (Ollama fallback)
requests

# Document generation
reportlab

# Computer vision — optional (app runs in demo mode without these)
opencv-python
# face-recognition   # requires dlib; install separately if needed
# dlib               # platform-specific build — see dlib-*.whl in repo root
